# step 11

# 요구사항
- 나의 시나리오에서 발생할 수 있는 동시성 이슈에 대해 파악하고 가능한 동시성 제어 방식들을 도입해보고 각각의 장단점을 파악한 내용을 정리 제출
    - 구현의 복잡도, 성능, 효율성 등
    - README 작성 혹은 외부 링크, 프로젝트 내의 다른 문서에 작성하였다면 README 에 링크 게재

# 동시성 이슈를 고려할 만한 기능
- 좌석 임시배정 
- 잔액 충전, 차감

# 동시성 이슈를 해결하기위해 고려한 키워드
issue keyword: race-condition, deadlock, dirty-read, non-repeatable-read, phantom read, db isolation
resolve keyword: redis setNx, 비관적락, 낙관적락, 분산락, 트랜잭션 manager, retry logic
+@ kafka 

## 좌석 임시 배정
1) 동시성 이슈가 왜 생길거 같은지?
현재로직 : 좌석 조회 시점에 hold 상태로 update하고 예약완료시 spring event를 발행해서 해당 좌석의 상태를 해제하는 식으로 구성되어있다. 
scheduler를 통해 만료시간이 지났지만 hold 상태가 해제되지 않은 좌석은 잠금을 해제하는 식으로 구현.

test시 이미 예약된 좌석이라는 message를 뱉으면서 실패하는 케이스가 존재 -> 이를 해결하기위해 lock을 사용.

2) 문제를 해결하기위해 어떤 부분을 고려하고 있는지?

2-1) 비관적 락
lock 
2-2) redis Rlock 
2-3) kafka event를 활용
kafka 내부적으로 같은 키로 이벤트 발행시 파티션의 내부구조가 queue로 이루어져 있어서 순차적인 처리가 가능함 + 낙관적 락 사용
redis에 점유한 임시좌석에 대한 data를 set해서 관리하고자 한다. , 혹은  
3) 문제를 어떻게 해결했는지?
4) 더 좋은 방법은 없었는지?

## 잔액 충전, 차감
1) 동시성 이슈가 왜 생길거 같은지?
유저가 동시에 여러번 요청을 하게되어 같은 정보를 여러번 조회하게되는 race condition이 생기지 않을까 생각한다.
2) 문제를 해결하기위해 어떤 부분을 고려하고 있는지?
현재 lockManager를 작성하여 Reentrant Lock을 활용하여 lock을 획득하면 리소스에 접근 가능하고 나머지는 실패처리하는 식으로 작성하였다.

-> redis의 분산락을 활용해서 구현했을때 어떤 차이가 있는지 useCase를 작성하여 확인예정. 

3) 문제를 어떻게 해결했는지?
4) 더 좋은 방법은 없었는지?
